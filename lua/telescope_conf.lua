require('telescope').setup{
  defaults = {
    prompt_prefix = "> ",
    selection_caret = "> ",
    path_display = { "smart" },
  },
  pickers = {
    -- You can customize individual pickers here (optional)
  },
  extensions = {
    -- Configuration for extensions (if you add any)
  }
}
